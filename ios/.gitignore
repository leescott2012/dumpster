# Xcode build artifacts
build/
build-device/
*.xcodeproj/project.xcworkspace/xcuserdata/
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.xcarchive

# SPM resolved (can be committed, but often .gitignored for solo projects)
# Packages.resolved

# Secrets — never commit API keys
DumpsterIOS/Secrets.swift

# macOS
.DS_Store
